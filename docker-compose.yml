services:
  postgres:
    image: postgres:15-alpine
    container_name: postgres
    restart: always
    environment:
      POSTGRES_USER: ${DB_USERNAME}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5433:5432"
    networks:
      - app-network

  redis:
    image: redis:7-alpine
    container_name: redis
    restart: always
    networks:
      - app-network

  backend:
    build: ./backend
    image: lephuchau/doctruyen-backend:latest
    container_name: doctruyen-backend
    restart: always
    env_file:
      - .env
    depends_on:
      - postgres
      - redis
    networks:
      - app-network

  shop_backend:
    build: ../ecommerce/backend
    image: lephuchau/shop-backend:latest
    container_name: shop-backend
    restart: always
    env_file:
      - .env
    networks:
      - app-network

  frontend:
    build:
      context: ./frontend
      args:
        NEXT_PUBLIC_API_ENDPOINT: https://lephuchau.online/backend # Build
        NEXT_PUBLIC_URL: https://lephuchau.online
    image: lephuchau/doctruyen-frontend:latest
    container_name: doctruyen-frontend
    restart: always
    env_file:
      - .env
    depends_on:
      - backend
    networks:
      - app-network

  nginx:
    image: nginx:alpine
    container_name: nginx
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/ssl:/etc/nginx/ssl:ro
    depends_on:
      - frontend
      - backend
    networks:
      - app-network

volumes:
  postgres_data:

networks:
  app-network:
